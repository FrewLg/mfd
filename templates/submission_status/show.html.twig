{% extends 'base.html.twig' %}

{% block title %}SubmissionStatus{% endblock %}

{% block body %}
    <h1>SubmissionStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ submission_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ submission_status.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ submission_status.description }}</td>
            </tr>
            <tr>
                <th>StatusColor</th>
                <td>{{ submission_status.statusColor }}</td>
            </tr>
            <tr>
                <th>StatusChangedAt</th>
                <td>{{ submission_status.statusChangedAt ? submission_status.statusChangedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_submission_status_index') }}">back to list</a>

    <a href="{{ path('app_submission_status_edit', {'id': submission_status.id}) }}">edit</a>

    {{ include('submission_status/_delete_form.html.twig') }}
{% endblock %}
